// SRDebugger Options — Camera controls (stub)
// Wire up once Cinemachine cameras are confirmed. Add a CameraSwitcher component
// to the scene and uncomment the block below.

using System.ComponentModel;

public partial class SROptions
{
    // private const string CamCat = "Camera";
    //
    // [Category(CamCat), Sort(0)]
    // [DisplayName("Active Camera")]
    // [NumberRange(0, 3)]
    // public int ActiveCamera
    // {
    //     get => BlobPreviz.CameraSwitcher.Instance?.ActiveIndex ?? 0;
    //     set { if (BlobPreviz.CameraSwitcher.Instance != null) BlobPreviz.CameraSwitcher.Instance.ActiveIndex = value; }
    // }
}
